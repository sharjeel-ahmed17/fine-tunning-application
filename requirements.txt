# ─────────────────────────────────────────────────────────────
# Fine-Tuning Chat App — Streamlit Cloud Compatible
# Python 3.14 compatible (Streamlit Cloud default)
# NOTE: torch, unsloth, bitsandbytes are installed at runtime
#       inside the app — NOT here — because they need Python
#       version-specific wheels resolved at install time.
# ─────────────────────────────────────────────────────────────

# Web Framework
streamlit>=1.40.0

# PDF Processing
pypdf>=4.3.0

# Web Scraping (html.parser used — no lxml system deps needed)
requests>=2.31.0
beautifulsoup4>=4.12.0
trafilatura>=1.9.0

# HuggingFace (pure-Python wheels, no CUDA needed to install)
transformers>=4.41.0
datasets>=2.20.0
accelerate>=0.31.0
peft>=0.11.0
trl>=0.9.0
huggingface-hub>=0.23.0
tokenizers>=0.19.0
sentencepiece>=0.2.0

# protobuf — must stay <5 for streamlit compatibility
protobuf>=3.20,<5

# Utilities
numpy>=1.26.0
pandas>=2.2.0
tqdm>=4.66.0
psutil>=6.0.0
