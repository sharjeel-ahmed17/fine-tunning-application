# ─────────────────────────────────────────────────────────────
# Fine-Tuning Chat App
# Python 3.12 pinned via .python-version file
# torch/unsloth/bitsandbytes installed at runtime in app.py
# ─────────────────────────────────────────────────────────────

# Web Framework
streamlit==1.45.1

# PDF Processing
pypdf==4.3.1

# Web Scraping (html.parser used — no lxml system deps needed)
requests==2.32.3
beautifulsoup4==4.12.3
trafilatura==1.9.0

# HuggingFace (pure-Python wheels)
transformers==4.41.2
datasets==2.20.0
accelerate==0.31.0
peft==0.11.1
trl==0.9.4
huggingface-hub==0.23.4
tokenizers==0.19.1
sentencepiece==0.2.0

# protobuf — MUST be pinned, <5 for streamlit + Python 3.12 C extension works
protobuf==3.20.3

# Utilities
numpy==1.26.4
pandas==2.2.2
tqdm==4.66.4
psutil==6.0.0
