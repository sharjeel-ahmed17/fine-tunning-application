# ─────────────────────────────────────────────────────────────
# Fine-Tuning Chat App — Streamlit Cloud
# Python 3.12 pinned via runtime.txt
# RULES: NO exact ==pins on compiled packages
#        NO sentencepiece, tokenizers, numpy, pandas pinned
#        All heavy ML (torch, unsloth) installed at runtime
# ─────────────────────────────────────────────────────────────

# Web Framework — pure Python wheel
streamlit>=1.40.0,<2.0.0

# PDF — pure Python wheel
pypdf>=4.0.0,<5.0.0

# Web scraping — pure Python wheels
requests>=2.28.0
beautifulsoup4>=4.12.0
trafilatura>=1.5.0

# HuggingFace — all pure Python (py3-none-any wheels)
huggingface-hub>=0.20.0
transformers>=4.36.0
datasets>=2.14.0
accelerate>=0.25.0
peft>=0.7.0
trl>=0.7.0
safetensors>=0.4.0

# protobuf pure-Python wheel (no C extension = no compile)
protobuf==3.20.3

# Utilities — NO version pin = pip picks pre-built wheel
numpy
pandas
tqdm
psutil
packaging

# tokenizers and sentencepiece removed from here
# → installed at runtime inside app.py alongside torch
